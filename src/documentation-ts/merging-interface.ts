//mergining interfaces

import { Cat } from '../A3-orientacao-objetos/PilaresPOO';

interface Box {
  height: number;
  width: number;
}

interface Box {
  scale: number;
}

const box: Box = { height: 5, width: 6, scale: 10 };

//the way more simple to explain how is the process of merging interfaces
