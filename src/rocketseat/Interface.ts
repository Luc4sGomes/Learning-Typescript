interface Usuario1 {
    id: number;
    name: string;
}

let newUser: Usuario1 = {
    id: 1,
    name: "lucas"
}

//usando interface para inferir o tipo dos dados, pode ser usado tanto o "Types" quanto interface
